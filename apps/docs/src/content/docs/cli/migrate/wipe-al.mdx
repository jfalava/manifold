---
title: wipe-al
description: Delete every AniList manga list entry and manga-related activity.
sidebar:
  order: 3
---

```text
bun run manifold migrate wipe-al [flags]
```

**Destructive.** Port of the AniList manga bulk-delete approach: scans your
AniList profile, then deletes **all** manga list entries and **all**
manga-related activities (list updates and text posts matching manga keywords).
Anime is never touched.

The command scans and reports what it would delete by default. `--apply` enables deletion and still prompts for confirmation. Pass `--yes` with `--apply` only when you intend to skip that prompt.

## Flags

| Flag              | Default | Notes                                            |
| ----------------- | ------- | ------------------------------------------------ |
| `--apply`         | `false` | Permanently delete the scanned AniList data      |
| `--yes`           | `false` | Skip confirmation when used with `--apply`       |
| `--anilist-token` | env     | `ANILIST_TOKEN` / `ALCHEMY_SECRET_ANILIST_TOKEN` |

## Usage

```text
bun run manifold migrate wipe-al
bun run manifold migrate wipe-al --apply
bun run manifold migrate wipe-al --apply --yes
```

An empty manga list exits cleanly without deleting anything.

<Aside type="danger" title="Irreversible">
  This permanently removes manga list state and activity history on AniList.
  Prefer exporting or migrating first if you still need the data.
</Aside>
