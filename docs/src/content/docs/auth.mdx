---
title: Authentication
description: Personal API, tracker, and upstream provider authentication.
sidebar:
  order: 2
---

Manifold keeps server-side upstream credentials on the personal Durable Object.
Tokens are encrypted before storage. Native Paperback providers manage their
own provider sessions separately.

## Supported providers

| Provider    | How you authenticate                                                                                    |
| ----------- | ------------------------------------------------------------------------------------------------------- |
| AniList     | Admin: browser implicit OAuth; CLI: [`login anilist`](/cli/login/anilist) (authorization-code loopback) |
| MyAnimeList | Admin: browser code flow; CLI: [`login mal`](/cli/login/mal) (PKCE loopback)                            |
| MangaDex    | Personal-client (password grant or personal token)                                                      |
| Comix       | Native Comix extension on the device                                                                    |

## Manifold API login

The tracker and CLI use a GitHub OAuth App to create a Manifold session. The
GitHub client secret stays in the Cloudflare Worker; Paperback stores only the
short-lived Manifold access token and rotated refresh token in secure state.
The CLI stores the same session in the OS keychain:

```text
manifold login manifold
```

Open the printed URL in a browser. The CLI listens only on
`127.0.0.1:8768/callback` while it completes the exchange. No Manifold API key
is needed for normal tracker or CLI use.

This deployment is personal rather than multi-tenant. Set
`MANIFOLD_GITHUB_ALLOWED_USER_ID` to the numeric GitHub user ID that owns the
instance; other GitHub accounts are rejected after authorization.

`MANIFOLD_TOKEN` and `--api-token` remain as an optional break-glass path for
administration and migration compatibility. They are not shown in tracker
settings and are not required after GitHub login.

Admin/API upstream OAuth (MAL code flow; AniList browser-minted token import)
stores encrypted tokens on the DO. Worker egress cannot call AniList reliably
(IP blocks), so AniList **writes** from migrations and `anilist:*` ops run on
your machine. The CLI keeps its own AniList/MAL sessions in the OS keychain via
[`login anilist`](/cli/login/anilist) and [`login mal`](/cli/login/mal) —
separate OAuth clients from the deployed API. MAL manga wipe:
[`mal wipe manga`](/cli/mal/wipe-manga).

## Provider sessions are provider-owned

Comix access is established by the native Comix extension inside Paperback.
Those cookies stay on the device. The CLI registry resolver may use a local
headed Chrome session for the same reason; it does not paste `cf_clearance`
into Worker `fetch`.

<Aside type="caution" title="Do not copy cookies into the Worker">
  Comix session cookies must not be uploaded to the personal API or canonical database. Keep them on
  the device that solved the check.
</Aside>
